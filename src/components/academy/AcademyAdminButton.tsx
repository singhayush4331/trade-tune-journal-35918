import React from 'react';

// This component is no longer needed as the admin button is now integrated into the AcademyHeader
// Keeping this file empty to avoid breaking imports, but it can be safely removed

const AcademyAdminButton: React.FC = () => {
  return null;
};

export default AcademyAdminButton;
